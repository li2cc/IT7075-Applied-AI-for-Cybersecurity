# Copy this file to ".env" (exact name) in your repo root, fill in real keys,
# and make sure ".env" is listed in .gitignore so it is NEVER committed.
#
#   cp sample.env .env      # then edit .env, not this file
#
# Keys are passwords that spend real money — never put them in code, notebooks,
# commits, or screenshots. If one leaks, rotate it immediately.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
