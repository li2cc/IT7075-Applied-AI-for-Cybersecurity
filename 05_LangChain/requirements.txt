# LangChain
# Core LangChain (v1.x) — used by both lecture notebooks
langchain>=1.0.0
langchain-core>=1.0.0
langchain-text-splitters>=1.0.0
langchain-community>=0.3.0

# RAG-with-LangChain (Lecture 3) + Mini-Project 3: vector store + local embeddings
langchain-chroma>=0.1.0
chromadb>=0.5.0
sentence-transformers>=2.2.0
langchain-huggingface>=0.1.0     # offline embeddings; Mini-Project 3 runs without an API key
numpy>=1.24.0
pandas>=2.0.0

# Live LLM step (optional; only needed to call a real model)
langchain-openai>=0.2.0
openai>=1.3.0
python-dotenv>=1.0.0
